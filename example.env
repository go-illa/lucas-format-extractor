# Example environment variables for the lucas-format-extractor project

# Groq API Key
GROQ_API_KEY="your_groq_api_key_here"

# AWS Credentials (for local testing)
# For Lambda, it's recommended to use an IAM role instead of storing credentials in environment variables.
AWS_REGION="your-aws-region"
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"

# AWS S3 Bucket for output
OUTPUT_S3_BUCKET="your-output-s3-bucket-name"